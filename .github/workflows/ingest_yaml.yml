name: Run Python Script1
 
on:
  push:
    branches:
      - main  # Replace with your branch name
    #paths:
     # - '**.xlsx' # Trigger when an Excel file is pushed
 
jobs:
  run-script:
    runs-on: windows-latest
 
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
 
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'  # Replace with your Python version
 
      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} # Using GitHub token for authentication
        run: |
      - name: Install pandas
        run: |
          python -m pip install openpyxl
          python -m pip install pandas  # Replace with the correct package name
          python -m pip install requests
          python -m pip install pyyaml
          #run: python -m pip install 
          curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }} " -o Data Taxonomy_workforce.xlsx -L https://github.com/puneet664/test_dbt_qw/blob/main/Data%20Taxonomy_workforce.xlsx
      - name: Run Python Script
        run: python excel_to_json.py  # Replace with your Python script name
      - name: Create glossary folder if it doesn't exist
        run: mkdir -p glossary_new
      - name: Create new file
        run: echo "Content for your new file" > glossary_new/new_file.yaml
 
      - name: List files in glossary folder (for verification)
        #run: ls -l glossary_new/
 
      - name: Display Git status
        run: git status
      - name: Commit files
        run: |
         git config --local user.email "action@github.com"
         git config --local user.name "GitHub Action"
 
         git add glossary_new/new_file.yml  # Stage the generated file within the existing folder

         git commit -m "Generated YAML from Excel"
         git push  main
